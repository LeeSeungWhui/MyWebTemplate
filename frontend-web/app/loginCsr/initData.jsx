/**
 * Login CSR route init data
 */
import { SESSION_PATH, createLoginFormModel } from '../login/initData';

export { SESSION_PATH, createLoginFormModel };

export const CSR_ENTRY_PROPS = {
  mode: 'CSR',
  init: null,
  nextHint: null,
};
