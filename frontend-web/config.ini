# 실행 환경별 설정은 config_dev.ini, config_prod.ini를 참고해 필요한 값으로 교체한다.
# 기본값은 개발 환경에 맞춘다.
[APP]
api_base_url = http://localhost:3000/api
cdn_base_url = http://localhost:3000/public

[FEATURE_FLAGS]
enable_mock = true
